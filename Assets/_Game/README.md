# _Game

프로젝트에서 직접 작성하거나 관리하는 코드와 콘텐츠만 둡니다.

```text
Core <- Client
Core <- Server
Core <- SOAP
Client/Server <- SOAP
Content: 씬, 프리팹, ScriptableObject 에셋
```

외부 패키지와 구매 에셋은 이 폴더의 코드를 직접 수정하지 않도록 별도 위치에 둡니다.
