# Server

Photon 연결, 방과 로비, 네트워크 권한, 상태 동기화와 경기 진행을 둡니다.

이 폴더의 Server는 별도 Spring 서버가 아니라 Unity 안의 Photon 네트워크 영역을 의미합니다.

- 참가자 모두에게 영향을 주는 결과는 권한을 가진 영역에서 확정합니다.
- 경기 시간은 Photon 서버 시간을 기준으로 계산합니다.
- 확정된 네트워크 상태를 로컬 R3 상태로 전달합니다.
- `Client`를 직접 참조하지 않습니다.
