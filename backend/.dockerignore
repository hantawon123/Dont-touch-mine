# 빌드 컨텍스트를 서버 데몬으로 통째로 전송하므로, 이미지에 들어가지
# 않는 것들은 애초에 보내지 않습니다.
build/
.gradle/

# 비밀번호가 들어 있습니다. 이미지에 절대 들어가면 안 됩니다.
.env

.git/
.gitignore
.gitattributes
README.md
compose.local.yml
compose.prod.yml
Dockerfile
